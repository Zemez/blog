= form_with(model: post, local: true) do |form|
  - if post.errors.any?
    #error_explanation
      h2
        = pluralize(post.errors.count, "error")
        |  prohibited this post from being saved:
      ul
        - post.errors.full_messages.each do |message|
          li
            = message
  .field.form-group
    = form.label :user, t('label.user') + ':'
    br
    = form.text_field :name, { value: @post_user.name.titlecase, readonly: true, class: 'form-control form-control col-md-2' }
    br
    = form.text_field :user_id, { readonly: true, type: current_user.moderator ? '': 'hidden', class: 'form-control form-control col-md-2' }
  .field.form-group
    = form.label :title, t('label.title') + ':'
    br
    = form.text_field :title, { class: 'form-control' }
  .field.form-group
    = form.label :body, t('label.body') + ':'
    br
    = form.text_area :body, { class: 'form-control' }
  .field.form-group
    = form.label :visible
      = form.check_box :visible, disabled: !current_user.moderator
      = t('label.visible')
  .actions.form-group
    = form.submit t('label.submit'), class: 'btn btn-primary btn-outline-success'
 